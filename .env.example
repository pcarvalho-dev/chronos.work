# Database Configuration
# OPTION 1: Use DATABASE_URL (recommended for production - Supabase, Render, etc.)
# DATABASE_URL=postgresql://user:password@host:5432/database

# OPTION 2: Use individual variables (recommended for local development)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=chronos_work
DB_SSL=false

# For Supabase in production, get the connection string from:
# Supabase Dashboard > Project Settings > Database > Connection String (URI)
# Example: postgresql://postgres.xxxxx:password@aws-0-us-east-1.pooler.supabase.com:5432/postgres

# Server Configuration
PORT=8000

# JWT Configuration
JWT_ACCESS_SECRET=your-access-token-secret-change-in-production
JWT_REFRESH_SECRET=your-refresh-token-secret-change-in-production

# Email Configuration (SMTP)
# Para desenvolvimento, você pode usar Ethereal Email (https://ethereal.email/)
# Para produção, use um serviço real como Gmail, SendGrid, AWS SES, etc.

# Ethereal Email (Desenvolvimento - Gere credenciais em https://ethereal.email/)
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_USER=your-ethereal-username
SMTP_PASS=your-ethereal-password
SMTP_FROM="Chronos.work <noreply@chronos.work>"

# Gmail Example (Produção - Requer App Password)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM="Chronos.work" <your-email@gmail.com>

# Frontend URL (para links em emails)
FRONTEND_URL=http://localhost:3000
